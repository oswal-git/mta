import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary
  static const Color primary = Colors.blue;
  static const Color onPrimary = Colors.white;
  static const Color secondary = Color(0xFF6750A4);
  static const Color onSecondary = Colors.white;

  // Neutral / Background
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF3F4F6);
  static const Color scaffoldBackground = Color(0xFFF9FAFB);

  // Status
  static const Color error = Color(0xFFB00020);
  static const Color success = Color(0xFF4CAF50);
  static const Color info = Color(0xFF1976D2);
  static const Color warning = Color(0xFFE65100); // orange[900]
  static const Color warningLight = Color(0xFFFFF3E0); // orange[100]

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textDisabled = Color(0xFF9CA3AF);

  // Borders
  static const Color border = Color(0xFFD1D5DB);

  // Static
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;
  static final Color? orange = Colors.orange[800];
}
