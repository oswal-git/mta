# Implementation Plan: Persistent Logging and Export Refinements - 2026-01-21 17:35

Implement persistent logging of assistant work and refine the export files to include user defaults and handle "Not indicated" zones.

## Proposed Changes

### [Component] Logging Infrastructure

1.  **New Folder**: `mta\tareas_gemini` will store timestamped Task and Implementation Plan files.

### [Component] Export System

#### [MODIFY] [export_params_entity.dart](file:///n:/flutter/mta/lib/features/export/domain/entities/export_params_entity.dart)

- Add `userBpMonitorModel` and `userMeasurementLocation` fields to include user defaults in the export parameters.

#### [MODIFY] [export_data_source.dart](file:///n:/flutter/mta/lib/features/export/data/datasources/export_data_source.dart)

- Update `exportToPDF` header to display the user's default Model and Zone.
- Adjust localization logic: if a zone is "Not indicated", return an empty string instead of the translation.

#### [MODIFY] [export_page.dart](file:///n:/flutter/mta/lib/features/export/presentation/pages/export_page.dart)

- Pass the active user's `bpMonitorModel` and `measurementLocation` to the `ExportParamsEntity`.
- Adjust the translations map to ensure "null" or "not_indicated" locations map to an empty string.

## Verification Plan

1. Check `n:\flutter\mta\tareas_gemini` for the created files.
2. Export a PDF and verify the header contains "Modelo" and "Zona".
3. Verify that measurements with "Sin determinar" zone appear with a blank space in Excel, CSV, and PDF.
